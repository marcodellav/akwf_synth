A simple wavetable synthesizer that can load single cycle waveforms

This project uses Scientific Pitch Notation (SPN): MIDI note 60 corresponds to C4 (Middle C).

This project uses AKWF-FREE as a git submodule.
After cloning, run `git submodule update --init --recursive`